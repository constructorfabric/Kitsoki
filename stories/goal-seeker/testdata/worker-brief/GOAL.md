# GOAL — worker-brief fixture

**One line:** one change (W1) with a mixed scope, used only by
flows/worker_brief_dispatch.yaml to prove the dispatched manifest's `prompt`
carries the WM.1 slim worker-brief projection.

## Done-when (criteria)

- **G1 — W1 lands.** A stub criterion the preamble surfaces.
