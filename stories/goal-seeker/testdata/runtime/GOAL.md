# GOAL — goal-seeker runtime-context fixture

**One line:** a tiny hermetic goal that declares a `runtime:` block, used only by
flows/runtime_context_resolves.yaml.

## Done-when (criteria)

- **G1 — First criterion.** A stub criterion the preamble surfaces.
