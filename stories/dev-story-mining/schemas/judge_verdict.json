{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title":   "judge_verdict",
  "description": "Structured verdict emitted by the checkpoint judge persona. `intent` is auto-fired above the confidence floor in llm / llm_then_human modes.",
  "type":    "object",
  "required": ["intent", "reason", "confidence"],
  "properties": {
    "intent":     { "type": "string", "enum": ["accept", "refine", "uncertain"], "description": "Which checkpoint intent the judge would fire." },
    "verdict":    { "type": "string", "description": "Free-text one-line verdict label (optional mirror of intent)." },
    "reason":     { "type": "string", "minLength": 4, "description": "Why — also threaded into refine feedback when intent=refine." },
    "confidence": { "type": "number", "minimum": 0, "maximum": 1, "description": "0–1; must be ≥ judge_confidence_threshold to auto-fire." }
  },
  "additionalProperties": true
}
