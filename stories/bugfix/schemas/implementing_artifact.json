{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title":   "implementing_artifact",
  "description": "Artifact produced by the implementing room: a record of the edits the agent applied to the bugfix workspace.",
  "type":    "object",
  "required": ["summary_title", "summary_markdown", "files_changed", "applied"],
  "properties": {
    "summary_title": {
      "type": "string",
      "minLength": 4,
      "description": "One-line description of what was implemented."
    },
    "summary_markdown": {
      "type": "string",
      "minLength": 1,
      "description": "Compact checkpoint note. The durable implementation narrative lives at report_path."
    },
    "report_path": {
      "type": "string",
      "minLength": 4,
      "description": "Workspace-relative report written during implementation, under .artifacts/bugfix/."
    },
    "files_changed": {
      "type": "array",
      "minItems": 1,
      "description": "Every file path the agent actually edited (or attempted to edit), relative to the workspace.",
      "items": {
        "type": "string",
        "pattern": "^[^/].+\\..+$"
      }
    },
    "applied": {
      "type": "boolean",
      "description": "True if every required edit was made successfully; false if the agent hit a blocker before completing the proposal."
    },
    "blockers": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Populated when applied=false. Each entry describes one reason an edit couldn't be made (file not found, conflict, ambiguous match, etc)."
    },
    "fallback_reason": {
      "type": "string",
      "enum": ["capability_missing", "task_not_decomposable"],
      "description": "Optional typed reason for the strict decomposed fallback treatment. The story retries only when this exact value is on the current work item's allowlist."
    },
    "notes": {
      "type": "string",
      "description": "Optional free-form notes the agent wants to leave for the testing-room reviewer."
    }
  },
  "additionalProperties": true
}
