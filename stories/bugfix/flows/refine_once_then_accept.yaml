# refine_once_then_accept — judge_mode=human, the operator refines the
# reproducing artifact once with feedback, then accepts on the re-run.
# Proves:
#   - refine intent self-transitions back to reproducing (not idle)
#   - world.refine_feedback carries the operator's note into the next
#     prompt invocation
#   - world.cycle increments

test_kind: flow
app: ../app.yaml
initial_state: idle
initial_world:
  bugfix_exit: "open-PR"   # legacy PR-handoff tail (slice 4 default is direct-ship)
  ticket_id:        "TKT-005"
  thread:          "TKT-005"
  judge_mode:      "human"

host_handlers:
  host.append_to_file:
    data: { ok: true, message_id: "msg-1" }
  host.inbox.add:
    data: { ok: true, id: "ib-1" }
  host.git_worktree:
    data: { ok: true, log: "" }
  host.git:
    data: { ok: true, sha: "abc123", diff: "" }
  host.local:
    data: { ok: true, log: "PASS", passed: 1, failed: 0 }
  host.agent.task:
    data:
      ok: true
      submitted:
        summary_title:    "Reproducer"
        summary_markdown: "A stub reproduction artifact."
        bug_verified:     true
        involved_components: [{ name: "tui" }]
        fix_description:  "Fix the thing in a sufficiently detailed way that the LLM-judge will accept it."
        root_cause:       "Off-by-one in the renderer; the listener observes the stale count."
        affected_files:   ["internal/tui/foo.go"]
        confidence:       0.85
        reasoning:        "Trace shows the off-by-one; well-trodden territory."
        status:           "passed"
        tests_run:        { passed: 1, failed: 0, log: "PASS" }
        outcome:          "pass"
        lessons:
          - { category: "stub", description: "A flow stub with enough body to satisfy the schemas loosely.", severity: "low" }

  host.agent.ask:
    data:
      ok: true
      submitted:
        summary_title:    "Reproducer"
        summary_markdown: "A stub reproduction artifact."
        bug_verified:     true
        involved_components: [{ name: "tui" }]
        fix_description:  "Fix the thing in a sufficiently detailed way that the LLM-judge will accept it."
        root_cause:       "Off-by-one in the renderer; the listener observes the stale count."
        affected_files:   ["internal/tui/foo.go"]
        confidence:       0.85
        reasoning:        "Trace shows the off-by-one; well-trodden territory."
        status:           "passed"
        tests_run:        { passed: 1, failed: 0, log: "PASS" }
        outcome:          "pass"
        lessons:
          - { category: "stub", description: "A flow stub with enough body to satisfy the schemas loosely.", severity: "low" }

  host.agent.decide:
    data:
      ok: true
      submitted:
        summary_title:    "Reproducer"
        summary_markdown: "A stub reproduction artifact."
        bug_verified:     true
        involved_components: [{ name: "tui" }]
        fix_description:  "Fix the thing in a sufficiently detailed way that the LLM-judge will accept it."
        root_cause:       "Off-by-one in the renderer; the listener observes the stale count."
        affected_files:   ["internal/tui/foo.go"]
        confidence:       0.85
        reasoning:        "Trace shows the off-by-one; well-trodden territory."
        status:           "passed"
        tests_run:        { passed: 1, failed: 0, log: "PASS" }
        outcome:          "pass"
        lessons:
          - { category: "stub", description: "A flow stub with enough body to satisfy the schemas loosely.", severity: "low" }

turns:
  - intent: { name: start, slots: {} }
    expect_state: reproducing
    expect_world:
      cycle: 0
      refine_feedback: ""

  # Operator refines with feedback. The state arc fires:
  #   target: reproducing (self-transition)
  #   effects: set refine_feedback, reproducing_cycle++, cycle++
  - intent:
      name: refine
      slots: { feedback: "Need a deterministic test, not a shell trace." }
    expect_state: reproducing
    expect_world:
      cycle:           1
      refine_feedback: "Need a deterministic test, not a shell trace."

  # accept resets cycle/refine_feedback and advances to proposing.
  - intent: { name: accept, slots: {} }
    expect_state: proposing
    expect_world:
      cycle:           0   # accept resets cycle on next-room transition
      refine_feedback: ""

  - intent: { name: accept, slots: {} }
    expect_state: implementing
  - intent: { name: accept, slots: {} }
    expect_state: testing
  - intent: { name: accept, slots: {} }
    expect_state: reviewing
  - intent: { name: accept, slots: {} }
    expect_state: validating
  - intent: { name: accept, slots: {} }
    expect_state: done
  - intent: { name: accept, slots: {} }
    expect_state: __exit__done
    expect_world: { status: "fixed" }

expect_terminal: true
expect_no_errors: true
