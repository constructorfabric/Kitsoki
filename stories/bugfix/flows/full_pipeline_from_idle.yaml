# full_pipeline_from_idle — `full_pipeline` is the explicit-mode default.
# Sets bugfix_mode="full" and routes to reproducing. Equivalent to `start`
# except it pins the mode flag (useful when a previous run left
# bugfix_mode="quick" in the carried world).

test_kind: flow
app: ../app.yaml
initial_state: idle
initial_world:
  ticket_id:   "TKT-420"
  thread:      "TKT-420"
  judge_mode:  "human"
  # Simulate a leftover quick from a previous run; full_pipeline overrides.
  bugfix_mode: "quick"

host_handlers:
  host.append_to_file: { data: { ok: true } }
  host.inbox.add:      { data: { ok: true } }
  host.capsule_workspace:   { data: { ok: true, log: "" } }
  host.git:            { data: { ok: true, sha: "abc", diff: "" } }
  host.local:          { data: { ok: true, log: "PASS", passed: 1, failed: 0 } }
  host.agent.task:
    data:
      ok: true
      submitted: { summary_title: "stub", summary_markdown: "stub" }

  host.agent.ask:
    data:
      ok: true
      submitted: { summary_title: "stub", summary_markdown: "stub" }

  host.agent.decide:
    data:
      ok: true
      submitted: { summary_title: "stub", summary_markdown: "stub" }

turns:
  - intent: { name: full_pipeline, slots: {} }
    expect_state: reproducing
    expect_world:
      bugfix_mode: "full"

expect_no_errors: true
