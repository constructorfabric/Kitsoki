# Generated by `kitsoki trace to-flow`.
# One episode per recorded host.* call, in trace order, matched on handler.
# Episodes are NOT replay:any, so the i-th call to a handler consumes the
# i-th matching episode — this reproduces per-call-varying responses.
kind: host_cassette
app_id: from-trace
generated_at: from-trace
match_on:
- handler
episodes:
- id: host_agent_codeact_1
  match:
    handler: host.agent.codeact
  response:
    data:
      payload:
        confidence: 0.97
        evidence: libs/toolkit/src/bootstrap/config/mod.rs:325 — `.merge(Env::prefixed("APP__").split("__"))` lowercases env segments but performs no underscore→hyphen translation. AppConfig.gears is `HashMap<String, serde_json::Value>` keyed by the literal gear name. libs/toolkit-macros/src/lib.rs:127 enforces gear names must use hyphens (kebab-case, not underscores), so a gear named `my-gear` is stored as key `"my-gear"`. An env var `APP__GEARS__MY_GEAR__CONFIG__KEY` resolves to path `gears.my_gear.config.key` (underscore, not hyphen), missing the map entry. No remap/translation function from underscore back to hyphen exists anywhere in the config loading pipeline. The only env-override test (line 2885) uses `env_test_vendor` (all underscores) and passes `APP__VENDOR__ENV_TEST_VENDOR__API_TOKEN` — there is no test for a hyphenated gear name. POSIX C_IDENTIFIERs forbid hyphens, so no valid env var can ever reach a `my-gear`-keyed entry.
        fixed_in_ref: null
        involved_components:
        - name: libs/toolkit/src/bootstrap/config/mod.rs
          reason: "Line 325: `Env::prefixed(\"APP__\").split(\"__\")` — the Figment env provider lowercases segments but does not translate underscores to hyphens; no map() callback applied. This is the root-cause site."
        - name: libs/toolkit-macros/src/lib.rs
          reason: "Lines 120-180: `validate_kebab_case` enforces gear names use hyphens, not underscores. Gear names like `file-parser` are stored with hyphen keys in the config map, making them unreachable via standard C_IDENTIFIER env vars."
        - name: libs/toolkit/src/bootstrap/config/mod.rs (test_vendor_config_env_override, line 2885)
          reason: Existing env-override test only uses underscore-named vendors (`env_test_vendor`), so it never exercises the kebab-case / hyphen gap. No regression test for hyphenated gear names exists.
        suggested_action: "Drive the full bugfix pipeline. Fix site: libs/toolkit/src/bootstrap/config/mod.rs:325 — add a `.map(|k| k.replace('_', \"-\"))` callback to the Figment Env provider (scoped to the gear-name segment), or post-process the loaded `gears` map to remap underscore-keyed entries to their hyphen equivalents. Add a regression test with a hyphenated gear name (e.g. `file-parser`) verifying that `APP__GEARS__FILE_PARSER__CONFIG__KEY=value` overrides `gears.file-parser.config.key`."
        summary_markdown: "## STILL-LIVE: Env var override silently ignored for any gear with a hyphen in its name\n\n### The collision\n\nGear names are enforced as **kebab-case** (e.g. `my-gear`) by the macro validator (`libs/toolkit-macros/src/lib.rs:127`: *\"Must not contain underscores\"*). The config loader builds the `AppConfig.gears` map with these exact string keys.\n\nThe Figment env-override layer (`libs/toolkit/src/bootstrap/config/mod.rs:325`) is:\n```rust\n.merge(Env::prefixed(\"APP__\").split(\"__\"));\n```\nFigment lowercases each `__`-delimited segment before matching. So `APP__GEARS__MY_GEAR__CONFIG__KEY=value` resolves to config path `gears.my_gear.config.key`. But the gear lives under key `\"my-gear\"` in the map — **different strings**; the override is silently dropped.\n\n### Why it can't be fixed by the operator\n\nPOSIX `C_IDENTIFIER` syntax forbids hyphens in environment variable names. There is no valid shell env var name that would produce a segment `my-gear` after splitting on `__`. The Figment `Env` provider has no `map` callback applied here to translate `_` → `-`.\n\n### No regression test\n\nThe only env-override test in the file (`test_vendor_config_env_override`, line 2885) uses a vendor named `env_test_vendor` — all underscores — so it sidesteps the issue entirely. There is no test asserting that a gear named, e.g., `file-parser` can have its config overridden via `APP__GEARS__FILE_PARSER__CONFIG__*`.\n\n### Fix location\n\nThe fix must add a `map` callback to the `Env` provider that translates underscores back to hyphens in the `gears.*` segment (or normalize the gear map keys to underscores on load). The canonical place is `load_layered` in `libs/toolkit/src/bootstrap/config/mod.rs` around line 325."
        summary_title: STILL-LIVE — Figment Env provider maps MY_GEAR→my_gear but kebab gear names are stored as my-gear; no translation exists
        verdict: STILL-LIVE
      steps:
      - error: "done() payload rejected: submit: schema validation failed. Fix these errors and call submit again:\n\n  - /involved_components/0: missing property 'name'\n  - /involved_components/1: missing property 'name'"
        observation: null
        snippet: ""
      - observation: null
        snippet: ""
      terminated: done
- id: host_append_to_file_post_2
  match:
    handler: host.append_to_file.post
  response:
    data:
      message_id: "4115#1"
      ok: true
