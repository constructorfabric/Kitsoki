# reproducing — phase 1 of the bugfix pipeline.
#
# Produces the `reproduction_artifact` (schemas/reproducing_artifact.json):
# evidence the bug is reproducible plus the components implicated.
#
# Design — one state per phase (no _executing / _awaiting_reply split).
# The Wave 1 contract split each phase into two states (an artifact-
# producing _executing and a transport-posting _awaiting_reply) to keep
# the shape symmetric across human and autonomous judge modes. In
# practice the split paid for that symmetry with confusion: in human
# mode the _awaiting_reply room had nothing for the operator to do
# beyond confirming a decision they'd already made; in autonomous
# mode the LLM judge could just as easily run in _executing's on_enter
# as in a separate room. Path B (this file) merges the two states.
#
# Per-mode behavior in the merged state:
#
#   human mode:
#     on_enter   → artifact agent binds reproduction_artifact.
#     view       → renders the artifact for the operator to read.
#     operator   → types `continue` (or any accept-alias) to advance,
#                  `refine feedback=…` to re-run with feedback,
#                  `restart_from stage=…` to bounce earlier,
#                  or `quit` to abandon.
#     accept arc → posts the artifact to the transport AND transitions
#                  to proposing. The post is a side-effect of the
#                  decision; the operator decides, then the post
#                  happens, then advance — all in one keystroke.
#
#   llm / llm_then_human:
#     on_enter   → artifact agent, THEN judge agent, THEN emit_intent
#                  the verdict (accept|refine|uncertain). All in the
#                  same turn. Human sees the room only if the judge's
#                  verdict was `uncertain` (llm_then_human falls back).
#
# inbox.add — REMOVED. It was firing a notification to an operator
# who was actively present in the room that emitted it. Inbox is the
# channel for things that happen WHILE YOU'RE ELSEWHERE: background
# jobs completing, CI results, async clarifications. If/when a phase
# moves to background: true the inbox.add can come back. The
# synchronous human-mode pipeline doesn't need it.

states:

  reproducing:
    description: "Reproduce the bug; produce the reproduction artifact for review."
    # Flattened view (no extends/blocks chrome) — `choice:` cannot live
    # inside a `blocks:` map. The base.pongo status line is inlined.
    view:
      - banner:
          text:     "REPRODUCING"
          subtitle: "Phase 1 / 7  ·  verify the bug; produce reproduction artifact"
          color:    "#06B6D4"   # cyan — cool start of the pipeline
      - prose: "{{ world.ticket_id|default:'(no ticket)' }} · cycle {{ world.cycle }} · {{ world.bugfix_mode }} bugfix · judge {{ world.judge_mode }}"
      - kv:
          pairs:
            Ticket:         "{{ world.ticket_id }} — {{ world.ticket_title }}"
            Summary:        '{{ world.reproduction_artifact.summary_title|default:"(pending)" }}'
            "Bug verified": '{{ world.reproduction_artifact.bug_verified|yesno:"yes,no,(pending)" }}'
      - heading: "Artifact"
      - template: '{{ world.reproduction_artifact.summary_markdown|default:"(pending — agent has not returned yet)" }}'
      - choice:
          mode: single
          prompt: "Actions"
          items:
            - label:  "continue"
              hint:   "post the artifact and advance to proposing"
              intent: accept
            - label:  "refine"
              hint:   "re-run with feedback"
              intent: refine
              param:
                slot:        feedback
                type:        string
                placeholder: "what to redo"
                required:    false
            - label:  "restart"
              hint:   "re-enter reproducing fresh (resets cycle)"
              intent: restart_from
              slots:  { stage: reproducing }
            - label:  "jump to testing"
              hint:   "skip ahead (audited)"
              intent: jump_to
              slots:  { stage: testing }
            - label:  "jump to validating"
              hint:   "skip ahead to validation (audited)"
              intent: jump_to
              slots:  { stage: validating }
            - label:  "jump to done"
              hint:   "skip to close-out (audited)"
              intent: jump_to
              slots:  { stage: done }
            - label:  "quit"
              hint:   "abandon the pipeline"
              intent: quit
            - label:  "look"
              hint:   "re-render"
              intent: look
    relevant_world: [judge_mode, judge_confidence_threshold, ticket_id, ticket_title, ticket_source_mode, ticket_source_ref, ticket_body, thread, workdir, test_cmd, reproduction_artifact, llm_verdict, refine_feedback, cycle, gate_command, repro_checked, regression_red_pre_fix, regression_pre_fix_log, repro_gate_ran, repro_gate_ok, repro_baseline_green, repro_test_red, repro_gate_summary, repro_gate_log]
    on_enter:
      # 0. Deterministic RED-gate (bugfix-repro-gate slice). When the ticket
      #    carried a `repro_command:` (projected into world.gate_command at
      #    ticket-load), prove the bug actually reproduces on the UNCHANGED
      #    (pre-fix) worktree BEFORE spending the LLM reproducer / maker budget.
      #    Structurally the cherny-loop baseline applied to the bugfix pipeline:
      #    run the command on HEAD and branch on exit code —
      #      RED  (non-zero exit) → bug reproduces → fall through to the LLM
      #                             reproducer below → proposing.
      #      GREEN (zero exit)    → cannot reproduce → @exit:not-reproducible.
      #    `once`-style latch via repro_checked: the gate runs once per session
      #    (refine/restart re-runs the LLM reproducer but not the gate — the
      #    unchanged worktree's repro verdict doesn't change). An empty
      #    gate_command skips the gate entirely (legacy ticket → LLM-only
      #    behaviour, unchanged — backward compatible).
      - when: "world.gate_command != '' && !world.repro_checked"
        invoke: host.run
        id: repro_gate_exec
        with:
          cmd: bash
          args:
            - "-c"
            - |
              set -e
              export NO_COLOR=1 GIT_TERMINAL_PROMPT=0 GIT_PAGER=cat
              GATE='{{ world.gate_command }}'
              GATE_EXIT=0
              GATE_OUT=$( bash -c "$GATE" 2>&1 ) || GATE_EXIT=$?
              if [ "$GATE_EXIT" -ne 0 ]; then
                # Non-zero exit — the bug reproduces (RED). Proceed.
                jq -n --arg log "$GATE_OUT" '{red:true,checked:true,log:$log}'
              else
                # Zero exit — the command passed (GREEN). Cannot reproduce.
                jq -n --arg log "$GATE_OUT" '{red:false,checked:true,log:$log}'
              fi
          cwd: "{{ world.workdir }}"
        bind:
          regression_red_pre_fix:  "stdout_json.red"
          repro_checked:           "stdout_json.checked"
          regression_pre_fix_log:  "stdout_json.log"
        on_error: idle

      # 0b. GREEN branch: the gate ran and the command PASSED on the unchanged
      #     worktree — the bug does not currently reproduce. Route to the
      #     needs-human `not-reproducible` exit BEFORE the LLM reproducer runs
      #     (the emit transitions away, so the agent step below is skipped).
      #     Guard reads repro_checked so the bind settles before routing.
      - when: "world.repro_checked && !world.regression_red_pre_fix"
        emit_intent: not_reproducible

      # 1. Always: produce the reproduction artifact. Schema-validated
      #    payload lands at Result.Data.submitted; bind walks
      #    "submitted" → world.reproduction_artifact.
      #    `working_dir` pins the agent subprocess to the bugfix
      #    worktree so any greps/reads the agent runs read the right
      #    tree. See implementing.yaml header for the regression context.
      # `once: true` for reload-safety: keyed off its own bind
      # (reproduction_artifact), so on /reload (or any self/on_error
      # re-entry) the stored artifact re-renders without re-running the
      # agent. The refine + restart_from arms below clear
      # reproduction_artifact to re-arm a fresh run.
      - invoke: host.agent.task
        once: true
        with:
          agent: reproducer
          working_dir: "{{ world.workdir }}"
          # A live reproducer is allowed to be slow, but never unbounded. The
          # supervised runtime records the timeout/cancel path in the trace so
          # an MCP driver can distinguish a real failure from a silent session
          # that remains running forever.
          sandbox:
            min_strength: supervised
            degrade: warn
            # Provider-backed worker profiles authenticate through the
            # operator's Claude home. Keep it for this explicitly live call.
            auth_home: inherit
            resources:
              timeout: "15m"
              activity_timeout: "90s"
          acceptance:
            schema: schemas/reproducing_artifact.json
          context:
            prompt: prompts/reproducing_executing.md
            args:
              ticket_id:        "{{ world.ticket_id }}"
              ticket_title:     "{{ world.ticket_title }}"
              ticket_source_mode: "{{ world.ticket_source_mode }}"
              ticket_source_ref: "{{ world.ticket_source_ref }}"
              ticket_body:      "{{ world.ticket_body }}"
              workdir:          "{{ world.workdir }}"
              refine_feedback:  "{{ world.refine_feedback }}"
              cycle:            "{{ world.cycle }}"
        bind:
          reproduction_artifact: submitted
        on_error: idle

      # 1b. DETERMINISTIC GREEN→RED gate — runs FIRST (before the LLM judge),
      #     entirely off the project `test_cmd` + the reproducer's authored test,
      #     which is provably ON DISK here (host.agent.task carries a durability
      #     barrier, so the reproducer's file writes have synced back to the dirty
      #     worktree before this step). It deliberately does NOT read the artifact's
      #     repro_command from world — a host.agent.task bind isn't visible to a
      #     later on_enter guard in the same turn — and it doesn't need to: the
      #     failing test is on disk. No pre-supplied oracle ⇒ leak-safe in a
      #     bake-off. Two legs in one legible event:
      #       RED   : run test_cmd on the DIRTY tree (the new, uncommitted test
      #               present) → must exit non-zero (the new test fails ⇒ the bug
      #               reproduces).
      #       GREEN : stash the reproducer's dirty changes, run test_cmd → must exit
      #               0 (the branch was clean before the test ⇒ the failure is NEW),
      #               then restore.
      #     repro_gate_ok = baseline_green && test_red. Only when it passes does the
      #     LLM judge run — and it then assesses TEST QUALITY/APPLICABILITY only, the
      #     GREEN→RED legs being mechanically proven. The GREEN(fix) leg is proven in
      #     `testing`. Scoped to the synthesis path (gate_command == ''); a
      #     ticket-supplied repro_command keeps the pre-agent RED gate (step 0).
      #     Empty test_cmd ⇒ skipped (LLM-only fallback). Latched by repro_gate_ran.
      #     See .context/green-red-green-repro-gate.md + flows/bugfix_green_red_green_gate.yaml.
      - when: "world.gate_command == '' && world.test_cmd != '' && world.workdir != '' && !world.repro_gate_ran"
        invoke: host.run
        id: repro_det_gate
        with:
          cmd: bash
          args:
            - "-c"
            - |
              set +e
              export NO_COLOR=1 GIT_TERMINAL_PROMPT=0 GIT_PAGER=cat
              # host.run already chdirs the child into cwd (world.workdir); do NOT
              # re-apply world.workdir here — it is a RELATIVE path in production
              # and a second cd/git -C would resolve it against the already-chdir'd
              # cwd (nested .worktrees/bf-<id>/.worktrees/bf-<id>) and self-skip.
              TESTCMD='{{ world.test_cmd }}'
              # RED leg: the suite on the DIRTY tree (reproducer's new failing test
              # present, no fix) must FAIL.
              RED_EXIT=0; RED_OUT=$( bash -c "$TESTCMD" 2>&1 ) || RED_EXIT=$?
              # GREEN baseline: stash the reproducer's dirty changes (everything but
              # the ownership sentinel), run the suite, restore. Must pass — the
              # branch was clean before the reproducer's test.
              STASHED=0
              if [ -n "$(git status --porcelain 2>/dev/null | grep -v '\.kitsoki-owner')" ]; then
                git stash push -u -m repro-gate-baseline >/dev/null 2>&1 && STASHED=1
              fi
              BASE_EXIT=0; BASE_OUT=$( bash -c "$TESTCMD" 2>&1 ) || BASE_EXIT=$?
              [ "$STASHED" = 1 ] && git stash pop >/dev/null 2>&1 || true
              RED=false;   [ "$RED_EXIT"  -ne 0 ] && RED=true
              GREEN=false; [ "$BASE_EXIT" -eq 0 ] && GREEN=true
              OK=false; { [ "$RED" = true ] && [ "$GREEN" = true ]; } && OK=true
              if   [ "$OK" = true ];     then SUM="✅ GREEN→RED proven · baseline GREEN (test_cmd exit 0) → reproducer test RED (test_cmd exit $RED_EXIT)"
              elif [ "$GREEN" != true ]; then SUM="❌ baseline NOT green — branch already failing (test_cmd exit $BASE_EXIT) without the reproducer test; gate cannot trust this branch"
              else                            SUM="❌ reproducer test did NOT fail (test_cmd exit 0) — bug not reproduced"
              fi
              RED_TAIL=$( printf '%s' "$RED_OUT"  | tail -c 1500 )
              BASE_TAIL=$( printf '%s' "$BASE_OUT" | tail -c 1500 )
              LOG=$( printf 'baseline(GREEN) test_cmd exit=%s | reproducer(RED) test_cmd exit=%s\n[reproducer/RED tail]\n%s\n[baseline/GREEN tail]\n%s' "$BASE_EXIT" "$RED_EXIT" "$RED_TAIL" "$BASE_TAIL" )
              jq -n --arg sum "$SUM" --arg log "$LOG" --argjson green $GREEN --argjson red $RED --argjson ok $OK \
                '{ran:true,baseline_green:$green,test_red:$red,ok:$ok,summary:$sum,log:$log}'
          cwd: "{{ world.workdir }}"
        bind:
          repro_gate_ran:       "stdout_json.ran"
          repro_baseline_green: "stdout_json.baseline_green"
          repro_test_red:       "stdout_json.test_red"
          repro_gate_ok:        "stdout_json.ok"
          repro_gate_summary:   "stdout_json.summary"
          repro_gate_log:       "stdout_json.log"
        on_error: idle

      # 1c. Post the deterministic verdict so it is legible in the trace + thread.
      - when: "world.repro_gate_ran && world.repro_gate_summary != ''"
        invoke: iface.transport.post
        with:
          thread:   "{{ world.thread }}"
          workdir:  "{{ world.workdir }}"
          title:    "Repro gate (GREEN→RED): {{ world.ticket_id }}"
          body:     "{{ world.repro_gate_summary }}\n\n{{ world.repro_gate_log }}"
          phase_id: "repro_gate_{{ world.ticket_id }}_{{ world.cycle }}"

      # 1d. A red baseline is a contaminated workspace/calibration input, not a
      #     deficient reproduction. It cannot be repaired by asking the same
      #     agent for another test, so fail closed with the gate diagnostic.
      - when: "world.repro_gate_ran && !world.repro_baseline_green"
        emit_intent: escalate_human
        slots:
          reason: "Strict reproduction gate found a contaminated baseline for {{ world.ticket_id }}. {{ world.repro_gate_summary }}\n\n{{ world.repro_gate_log }}"

      # 1e. With a known-good baseline, a reproducer test that does not go RED
      #     is actionable agent feedback and may use the bounded refine loop.
      - when: "world.repro_gate_ran && world.repro_baseline_green && !world.repro_gate_ok"
        emit_intent: refine
        slots:
          feedback: "{{ world.repro_gate_summary }}"

      # 2. Conditionally: run the LLM judge against the artifact. It assesses TEST
      #    QUALITY/APPLICABILITY only — the GREEN→RED legs are PROVEN deterministically
      #    above. Guarded so it runs only when the deterministic gate passed
      #    (repro_gate_ok) OR was skipped (no test_cmd / ticket-supplied gate_command
      #    ⇒ LLM-only fallback). Skipped in human mode (operator IS the judge).
      - when: |
          (world.judge_mode == 'llm' || world.judge_mode == 'llm_then_human') &&
          (world.test_cmd == '' || world.gate_command != '' || world.repro_gate_ok)
        invoke: host.agent.decide
        with:
          agent: judge
          prompt: prompts/judge_reproducing.md
          schema: schemas/judge_verdict.json
          working_dir: "{{ world.workdir }}"
          args:
            ticket_id:       "{{ world.ticket_id }}"
            artifact_title:  "{{ world.reproduction_artifact.summary_title }}"
            artifact_body:   "{{ world.reproduction_artifact.summary_markdown }}"
        bind:
          llm_verdict: submitted

      # 3. Auto-fire the judge's verdict above the confidence floor.
      #    `uncertain` verdicts fall through to the human-mode view.
      #    Bounded by machine.EmitIntentMaxDepth (=8).
      - when: |
          world.judge_mode != 'human' &&
          (world.llm_verdict.verdict ?? '') != '' &&
          (world.llm_verdict.confidence ?? 0) >= world.judge_confidence_threshold &&
          world.llm_verdict.verdict != 'uncertain' &&
          world.llm_verdict.intent != 'uncertain'
        emit_intent: "{{ world.llm_verdict.intent }}"
        slots:
          feedback: "{{ world.llm_verdict.reason }}"
    on:
      # Internal strict baseline failure. Keep the operational distinction
      # visible: this is a contaminated workspace/bad benchmark baseline, not
      # an unverified product bug or an exhausted agent budget.
      escalate_human:
        - target: "@exit:needs-human"
          effects:
            - set:
                needs_human_reason: "{{ slots.reason }}"
                last_error:         "NEEDS HUMAN (reproducing baseline): {{ slots.reason }}"
                status:             "needs-human"

      # `accept` advances to proposing. Examples include "continue" and
      # "proceed" so the LLM router maps the canonical user verbs here.
      # The transport.post is a side-effect of the decision — the
      # operator decides, the artifact gets durably recorded, the
      # session advances. One arc, one keystroke.
      accept:
        # Advance to proposing. The SYNTHESISED-GATE work (commit the reproducer's
        # RED test as the discrete pre-fix reproducer + derive gate_command) runs
        # in PROPOSING's on_enter, not here: the reproducer is an async agent whose
        # file write + artifact bind are only durable a TURN LATER, so committing on
        # this same-turn accept (the llm auto-accept path) raced the file flush and
        # silently committed nothing. proposing/on_enter runs after the dust settles.
        - target: proposing
          effects:
            - invoke: iface.transport.post
              with:
                thread:   "{{ world.thread }}"
                workdir:  "{{ world.workdir }}"
                title:    "Reproduction artifact: {{ world.ticket_id }}"
                body:     "{{ world.reproduction_artifact.summary_markdown }}"
                phase_id: "reproducing_{{ world.ticket_id }}_{{ world.cycle }}"
            - set:
                last_reply_author: "{{ slots.author ?? 'human' }}"
                refine_feedback:   ""
                cycle:             0

      # L2 cycle-budget gate. Refining past the budget routes to
      # @exit:abandoned with a structured reason instead of looping.
      refine:
        # Active escalation: a reproduction that keeps failing the GREEN→RED gate
        # (never behaviorally arms — the #47 mis-scope signal) quits to a human
        # with the gate output instead of silently abandoning.
        - when: "world.escalate_low_value && world.reproducing_cycle >= world.reproducing_budget"
          target: "@exit:needs-human"
          effects:
            - set:
                needs_human_reason: "Reproduction never cleanly armed the GREEN→RED gate for {{ world.ticket_id }} after {{ world.reproducing_cycle }} cycles — likely mis-scoped / not behaviorally reproducing the reported symptom. {{ world.repro_gate_summary }}"
                last_error:         "NEEDS HUMAN (reproducing): repro gate never armed after {{ world.reproducing_cycle }} cycles."
                status:             "needs-human"
        - when: "world.reproducing_cycle >= world.reproducing_budget"
          target: "@exit:abandoned"
          effects:
            - set:
                abandon_reason: "reproducing_cycle_budget_exhausted"
                status:         "abandoned"
        - target: idle
          effects:
            - set:
                refine_feedback:        "{{ slots.feedback ?? world.llm_verdict.reason }}"
                reproducing_cycle:      "{{ world.reproducing_cycle + 1 }}"
                cycle:                  "{{ world.cycle + 1 }}"
                # Leave and re-enter via idle so reproducing.on_enter fires again.
                # A same-state reproducing -> reproducing transition only applied
                # these effects and left the artifact pending with no agent job.
                bf_autostart_attempted: false
                # Clear the once: bind so on_enter re-runs the agent.
                reproduction_artifact:  {}
                # Re-arm the deterministic GREEN→RED gate for the fresh artifact.
                repro_gate_ran:         false
                repro_gate_ok:          false
                repro_gate_summary:     ""

      # restart_from re-enters this phase fresh; nothing earlier to
      # rewind to from reproducing.
      restart_from:
        - target: idle
          effects:
            - set:
                restart_from_stage:    "{{ slots.stage ?? 'reproducing' }}"
                refine_feedback:       ""
                reproducing_cycle:     0
                cycle:                 0
                # Re-arm idle's auto-start so the fresh reproducing room is
                # actually entered and its on_enter agent call re-runs.
                bf_autostart_attempted: false
                # Clear the once: bind so on_enter re-runs the agent fresh.
                reproduction_artifact: {}
                # Re-arm the deterministic GREEN→RED gate.
                repro_gate_ran:        false
                repro_gate_ok:         false
                repro_gate_summary:    ""

      # jump_to skips ahead. Audited via world.unsafe_jumps_made.
      jump_to:
        - when: "slots.stage == 'testing' || slots.stage == 'test'"
          target: testing
          effects:
            - set:
                jump_to:           "{{ slots.stage }}"
                unsafe_jumps_made: "{{ world.unsafe_jumps_made + 1 }}"
        - when: "slots.stage == 'validating' || slots.stage == 'validate'"
          target: validating
          effects:
            - set:
                jump_to:           "{{ slots.stage }}"
                unsafe_jumps_made: "{{ world.unsafe_jumps_made + 1 }}"
        - when: "slots.stage == 'done' || slots.stage == 'pr'"
          target: done
          effects:
            - set:
                jump_to:           "{{ slots.stage }}"
                unsafe_jumps_made: "{{ world.unsafe_jumps_made + 1 }}"
        - target: "@exit:abandoned"
          effects:
            - set:
                abandon_reason: "jump_to_unknown_stage"
                status:         "abandoned"

      # Mid-flow mode switches. quick_fix flips bugfix_mode without
      # state movement; skip_to_pr jumps to validating; full_pipeline
      # restores the default.
      quick_fix:
        - target: .
          effects:
            - set:
                bugfix_mode: "quick"
      full_pipeline:
        - target: .
          effects:
            - set:
                bugfix_mode: "full"
      skip_to_pr:
        - target: validating
          effects:
            - set:
                bugfix_mode:        "full"
                restart_from_stage: "validate"
                unsafe_jumps_made:  "{{ world.unsafe_jumps_made + 1 }}"

      # repro RED-gate GREEN branch (bugfix-repro-gate slice). The
      # deterministic repro_command PASSED on the unchanged worktree, so the
      # bug does not currently reproduce. Hand off to needs-human with the
      # gate's captured output as the reason; never spend maker budget.
      not_reproducible:
        - target: "@exit:not-reproducible"
          effects:
            - set:
                last_error: "repro_command passed (GREEN) on the unchanged worktree — bug does not currently reproduce.\n{{ world.regression_pre_fix_log }}"
                status:     "not-reproducible"

      quit:
        - target: "@exit:abandoned"
      look:
        - target: .
