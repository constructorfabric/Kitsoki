inputs:
  bucket_key:   { type: string, required: true }
  media_handle: { type: string, required: false }
  reason:       { type: string, required: false }

outputs:
  bucket:       { type: object }
  report_lines: { type: string }
