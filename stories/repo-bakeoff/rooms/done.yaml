# done — gallery. The bake-off produced a rendered slidey report deck. Show the
# final media + the headline rollup. Terminal: `accept` lifts @exit:done (which
# requires deck_handle — a real rendered report exists). Deterministic, no host
# calls.

states:

  done:
    description: "Bake-off complete — the report deck is rendered."
    relevant_world: [completion_status, completion_markdown, deck, deck_handle, summary, report_markdown]
    view:
      - heading: "Bake-off · done"
      - media:
          handle: "{{ world.deck_handle }}"
      - kv:
          pairs:
            Completion: '{{ world.completion_status|default:"(unknown)" }}'
            Completion report: '{{ world.completion_markdown|default:"(not generated)" }}'
            Report:  '{{ world.deck.summary }}'
            Deck:    '{{ world.deck_handle }}'
            Cells:   "{{ world.summary.cells|length }}"
      - prose: "The external-repo bake-off report deck is rendered with the completion verdict preserved for review. Accept to finish."
      - choice:
          mode: single
          prompt: "Actions"
          items:
            - label:  "accept"
              hint:   "finish (@exit:done)"
              intent: accept
            - label:  "look"
              hint:   "re-render"
              intent: look
    on:
      accept:
        - target: "@exit:done"
      look:
        - target: .
