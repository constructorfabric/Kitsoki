# Replay recording for the off-ramp demo.
#
# A single CLARIFY entry: in the `desk` room, the free-text utterance
# "why should I trust an AI with my project?" maps to NO declared intent
# (the menu is browse/status/about — none of them answer this). `clarify: true`
# makes the replay harness return a *ClarifyResponse (a deterministic no-match)
# instead of an intent, which drives the orchestrator's clarify branch — and,
# because the `desk` room declares `agent_off_ramp:`, fires the off-ramp:
# the question is handed to host.agent.converse (stubbed by
# assets/converse-cassette.yaml) and the answer comes back as a ModeOffPath
# turn. The room stays put; the menu is unchanged next turn.
#
# Menu picks (browse/status/about/back/quit) are EXPLICIT intents submitted via
# runstatus.session.submit — they need no recording entry and no harness.
kind: recording
app_id: off-ramp-demo
app_version: 0.1.0
generated_at: "2026-06-15T00:00:00Z"
generator: "hand-authored (off-ramp demo)"
min_confidence: 1.00

entries:
  - state: desk
    input: "why should I trust an AI with my project?"
    clarify: true
    message: "I don't have a menu item for that — let me just answer it."
