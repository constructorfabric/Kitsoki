# test — phase 3 of the implementation pipeline. Runs the workspace
# tests via iface.ci.run_tests, captures the log, and the LLM produces
# a `test_artifact` (schemas/test_artifact.json) summarising the run.
# The checkpoint gates pass/fail: accept → review; refine → write_code
# with the failing-test feedback so the code-write loop closes around
# CI failures.
#
# Flattened away from `extends: "base"` (choice-widget proposal §4.5 /
# §8): a `choice:` element cannot live inside `blocks:`. The base.pongo
# status line is inlined as the leading prose element.

states:

  test_executing:
    description: "Running tests for the applied code change."
    view:
      - banner:
          text:     "🧪 RUNNING TESTS"
          subtitle: "CI and test-review agent are active"
          color:    "#F59E0B"
      - prose: '{{ world.ticket_id|default:"(no ticket)" }}  ·  {{ world.workdir|default:"(no workspace)" }}  ·  {{ world.feature_branch|default:"(no branch)" }}  ·  cycle {{ world.cycle }}  ·  judge {{ world.judge_mode }}'
      - kv:
          pairs:
            Ticket:     "{{ world.ticket_id }} — {{ world.ticket_title }}"
            Summary:    '{{ world.test_artifact.summary_title|default:"agent working..." }}'
            "CI state": '{{ world.ci_state|default:"(pending)" }}'
            Status:     "running"
      - template: |
          **Tests are running now.** Results will settle into a short checkpoint with a linked artifact.
      - choice:
          mode: single
          prompt: "Actions"
          items:
            - { label: "proceed", intent: proceed, hint: "deliver the test artifact to the checkpoint", when: "world.judge_mode != 'human'" }
            - label:  "restart_from"
              intent: restart_from
              hint:   "bounce back to an earlier stage"
              param:
                slot:        stage
                type:        string
                placeholder: "e.g. review_task"
            - { label: "quit", intent: quit, hint: "abandon the pipeline" }
            - { label: "look", intent: look, hint: "re-render" }
    relevant_world: [ticket_id, ticket_title, test_artifact, code_artifact, cycle, ci_state]
    on_enter:
      - invoke: iface.ci.run_tests
        with:
          workdir: "{{ world.workdir }}"
          target:  "{{ world.code_artifact.summary_title }}"
        bind:
          ci_log:   log
          ci_state: state
        on_error: idle
      # once: skip the regenerate when test_artifact is already bound
      # (reload / self-re-entry safe). Every arc that re-enters
      # test_executing for a FRESH run clears the bind below.
      - invoke: host.agent.decide
        once: true
        with:
          prompt: prompts/judge_test.md
          schema: schemas/test_artifact.json
          args:
            ticket_id:    "{{ world.ticket_id }}"
            ticket_title: "{{ world.ticket_title }}"
            code_summary: "{{ world.code_artifact.summary_markdown }}"
            ci_log:       "{{ world.ci_log }}"
            ci_state:     "{{ world.ci_state }}"
        bind:
          test_artifact: submitted
        on_error: idle

      # ── Commit the test phase's work (lost-work guard) ─────────────────
      # The test agent (above) may write new tests or repair files directly
      # in the maker worktree, but NOTHING downstream commits them —
      # write_code only committed its own change, and the gates run against
      # the live (dirty) worktree, so a test or in-place repair that lives
      # only in the working tree passes CI yet never reaches the committed
      # tip. That is the lost-work split: a broken/partial HEAD shipped while
      # the real work sat uncommitted. `stage_all: true` runs `git add -A`
      # so new untracked test files AND in-place repairs both land. NOT
      # `once:` — a refine pass re-runs the test phase and must re-commit its
      # new edits; an unchanged tree is a graceful no-op (git_vcs.go treats
      # "nothing to commit" as success). Guarded on a worktree existing so
      # narrative-only walks (no isolated worktree) skip it. The `handoff`
      # room's clean-tree guard is the backstop.
      - when: "world.workdir != ''"
        invoke: iface.vcs.commit
        with:
          workdir:   "{{ world.workdir }}"
          message:   "test: {{ world.ticket_id }} — tests + fix repairs from test phase"
          stage_all: true
        on_error: idle
      - invoke: host.artifacts_dir
        id: test_artifact_file
        with:
          thread: "implementation_{{ world.ticket_id }}_03_tests.md"
          title:  "Tests: {{ world.ticket_id }}"
          body:   "{{ world.test_artifact.summary_markdown }}"
          mode:   replace
        bind:
          test_artifact_path: path
        on_error: idle
      - invoke: host.artifacts_dir
        id: implementation_progress_file
        with:
          thread: "implementation_{{ world.ticket_id }}_progress.yaml"
          body: |
            ticket_id: "{{ world.ticket_id }}"
            phase: test
            state: ready_for_operator
            cycle: {{ world.cycle }}
            ci_state: "{{ world.ci_state }}"
            test_status: "{{ world.test_artifact.status }}"
            artifacts:
              task_summary: "{{ world.task_summary_artifact_path }}"
              code: "{{ world.code_artifact_path }}"
              test: "{{ world.test_artifact_path }}"
              review: "{{ world.review_artifact_path }}"
            next_action: accept_or_refine
          mode: replace
        bind:
          implementation_progress_artifact_path: path
        on_error: idle
      - when: "world.judge_mode == 'human' && world.test_artifact_path != ''"
        emit_intent: proceed
    on:
      proceed:
        - target: test_awaiting_reply
      restart_from:
        - target: review_task_executing
          effects:
            - set:
                restart_from_stage: "{{ slots.stage ?? 'review_task' }}"
                cycle:              0
                # Re-entering review_task_executing for a fresh run — clear
                # its once: cache so the task summary regenerates.
                task_summary_artifact: {}
                task_summary_artifact_path: ""
      quit:
        - target: "@exit:abandoned"
      look:
        - target: .

  # Canonical §6 checkpoint shape.
  test_awaiting_reply:
    description: "Test results posted; awaiting verdict."
    view:
      - when: "world.test_artifact.status == 'failed' || world.test_artifact.status == 'blocked'"
        banner:
          text:     "⚠ TESTS NEED WORK"
          subtitle: "Accept will loop back to code; refine can add specific feedback"
          color:    "#DC2626"
      - when: "world.test_artifact.status != 'failed' && world.test_artifact.status != 'blocked'"
        banner:
          text:     "✅ TEST RESULTS READY"
          subtitle: "Review the short result; open the artifact for details"
          color:    "#16A34A"
      - prose: '{{ world.ticket_id|default:"(no ticket)" }}  ·  {{ world.workdir|default:"(no workspace)" }}  ·  {{ world.feature_branch|default:"(no branch)" }}  ·  cycle {{ world.cycle }}  ·  judge {{ world.judge_mode }}'
      - kv:
          pairs:
            Ticket:   "{{ world.ticket_id }} — {{ world.ticket_title }}"
            Summary:  '{{ world.test_artifact.summary_title|default:"(missing title)" }}'
            Status:   '{{ world.test_artifact.status|default:"(unknown)" }}'
            "CI state": '{{ world.ci_state|default:"(unknown)" }}'
            Artifact: '{{ world.test_artifact_path|default:"(not written)" }}'
            Progress: '{{ world.implementation_progress_artifact_path|default:"(not written)" }}'
      - when: "(world.llm_verdict.verdict ?? '') != ''"
        kv:
          pairs:
            Verdict:    '{{ world.llm_verdict.verdict }}'
            Confidence: '{{ world.llm_verdict.confidence|floatformat:2 }}'
            Reason:     '{{ world.llm_verdict.reason }}'
      - template: |
          **{{ world.test_artifact.summary_title|default:"Test result" }}**

          {% if world.test_artifact.status == 'failed' || world.test_artifact.status == 'blocked' %}⚠ **Action needed:** this result will loop back to code unless you refine with different feedback.{% else %}The detailed run log and review notes are in the linked artifact.{% endif %}
      - choice:
          mode: single
          prompt: "Actions"
          items:
            - { label: "accept", intent: accept, hint: "advance to review" }
            - label:  "refine"
              intent: refine
              hint:   "bounce back to write_code with failing-test feedback"
              param:
                slot:        feedback
                type:        string
                placeholder: "what to change"
            - label:  "restart_from"
              intent: restart_from
              hint:   "bounce back to an earlier stage"
              param:
                slot:        stage
                type:        string
                placeholder: "e.g. review_task"
            - { label: "quit", intent: quit, hint: "abandon the pipeline" }
            - { label: "look", intent: look, hint: "re-render" }
    relevant_world: [judge_mode, ticket_id, test_artifact, ci_state, llm_verdict, cycle]
    on_enter:
      - invoke: iface.transport.post
        with:
          thread:    "{{ world.thread }}"
          title:     "Tests: {{ world.ticket_id }}"
          body: |
            Test result ready: {{ world.test_artifact.summary_title }}
            Status: {{ world.test_artifact.status }}
            CI: {{ world.ci_state }}

            Full artifact: {{ world.test_artifact_path }}
            Progress: {{ world.implementation_progress_artifact_path }}
          phase_id:  "test_{{ world.ticket_id }}_{{ world.cycle }}"
      - invoke: host.inbox.add
        with:
          kind:    checkpoint
          title:   "Tests: {{ world.ticket_id }}"
          thread:  "{{ world.thread }}"
          state:   test_awaiting_reply
          body:    "Test result ready: {{ world.test_artifact.summary_title }}\nStatus: {{ world.test_artifact.status }}\nCI: {{ world.ci_state }}\n\nFull artifact: {{ world.test_artifact_path }}\nProgress: {{ world.implementation_progress_artifact_path }}"
      - when: "world.judge_mode == 'llm' || world.judge_mode == 'llm_then_human'"
        invoke: host.agent.decide
        with:
          prompt: prompts/judge_test.md
          schema: schemas/judge_verdict.json
          args:
            ticket_id:       "{{ world.ticket_id }}"
            artifact_title:  "{{ world.test_artifact.summary_title }}"
            artifact_body:   "{{ world.test_artifact.summary_markdown }}"
            ci_state:        "{{ world.ci_state }}"
        bind:
          llm_verdict: submitted
      - when: |
          world.judge_mode != 'human' &&
          world.llm_verdict.confidence >= world.judge_confidence_threshold &&
          world.llm_verdict.verdict != 'uncertain' &&
          world.llm_verdict.intent != 'uncertain'
        emit_intent: "{{ world.llm_verdict.intent }}"
        slots:
          feedback: "{{ world.llm_verdict.reason }}"
    on:
      # accept routes by test_artifact.status (the schema enum
      # passed/failed/blocked). A failed/blocked verdict must NOT advance to
      # review — it bounces back to write_code with the failing-test feedback
      # (mirrors bugfix testing's status-aware accept). Without this branch
      # accept advanced UNCONDITIONALLY, broadcasting a failed test run as
      # success. A passed verdict (or an artifact with no status — narrative
      # walks / older fixtures) advances to review.
      accept:
        # failed / blocked — fix needs another pass; bounce to write_code.
        - when: |
            world.test_artifact.status == 'failed' ||
            world.test_artifact.status == 'blocked'
          target: write_code_executing
          effects:
            - set:
                refine_feedback: "{{ world.test_artifact.summary_markdown }}"
                cycle:           "{{ world.cycle + 1 }}"
                # Clear the once: cache so the code artifact regenerates.
                code_artifact:   {}
                code_artifact_path: ""
        # passed (or unset) — advance to review.
        - target: review_executing
          effects:
            - set:
                last_reply_author: "{{ slots.author ?? 'human' }}"
                refine_feedback:   ""
                cycle:             0
                # Advancing to review_executing for a fresh run — clear its
                # once: cache so the review artifact regenerates.
                review_artifact:   {}
                review_artifact_path: ""
      # CI failure path: refine bounces back to write_code with the
      # failing-test feedback so the code-write loop closes around CI.
      refine:
        - target: write_code_executing
          effects:
            - set:
                refine_feedback: "{{ slots.feedback ?? world.llm_verdict.reason }}"
                cycle:           "{{ world.cycle + 1 }}"
                # Clear the once: cache so the code artifact regenerates.
                code_artifact:   {}
                code_artifact_path: ""
      restart_from:
        - target: review_task_executing
          effects:
            - set:
                restart_from_stage: "{{ slots.stage ?? 'review_task' }}"
                cycle:              0
                # Re-entering review_task_executing for a fresh run — clear
                # its once: cache so the task summary regenerates.
                task_summary_artifact: {}
                task_summary_artifact_path: ""
      quit:
        - target: "@exit:abandoned"
      look:
        - target: .
