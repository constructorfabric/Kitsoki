{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title":   "fix_artifact",
  "description": "Record produced by the fixer for one cycle: what failed, what was changed, and whether a human decision is required.",
  "type":    "object",
  "required": ["summary_title", "summary_markdown", "report_path", "files_changed", "fixed_tests", "remaining_failures", "needs_decision", "confidence"],
  "properties": {
    "summary_title": {
      "type": "string",
      "minLength": 4,
      "description": "One-line description of what this cycle did (e.g. 'Fixed nil deref in run handler exit-code path')."
    },
    "summary_markdown": {
      "type": "string",
      "minLength": 1,
      "description": "Compact cycle note; the durable diagnosis and repair record is report_path."
    },
    "report_path": {
      "type": "string",
      "minLength": 4,
      "description": "Workspace-relative repair report written during this cycle, under .artifacts/fix-tests/."
    },
    "files_changed": {
      "type": "array",
      "description": "Every file the fixer edited this cycle, repo-relative. Empty if no edit was made (e.g. needs_decision=true).",
      "items": { "type": "string" }
    },
    "fixed_tests": {
      "type": "array",
      "description": "Tests the fixer believes it has now made pass.",
      "items": { "type": "string" }
    },
    "remaining_failures": {
      "type": "array",
      "description": "Failing tests the fixer could NOT resolve this cycle (still expected to fail).",
      "items": { "type": "string" }
    },
    "needs_decision": {
      "type": "boolean",
      "description": "TRUE when a failure can only be resolved by a product/architecture decision the fixer must not make alone (ambiguous intended behaviour, a destructive or wide-reaching change, a genuinely flaky/environmental failure). When true, do NOT edit files — populate open_questions and stop."
    },
    "open_questions": {
      "type": "array",
      "description": "Populated when needs_decision=true. Each entry is one concrete question a human must answer before the fix can proceed.",
      "items": { "type": "string" }
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "The fixer's confidence (0-1) that this cycle's change makes the targeted tests pass."
    }
  },
  "additionalProperties": true
}
