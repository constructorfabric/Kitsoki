// panel.mjs — a trivial ESM stub standing in for a real Vite lib-mode kit UI
// build (S3c). It default-exports the {pages, nav} shape D3 describes, but
// this fixture never actually gets dynamic-imported by a running SPA yet —
// it only proves the /kit/<ns>/<kit>/ui/* static route serves the right
// bytes with the right content-type.
export default { pages: [], nav: [] };
