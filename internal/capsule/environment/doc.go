// Package environment resolves checked-in Capsule environment definitions into
// content-addressed locks. Resolution observes and records tool/image inputs;
// it never installs or upgrades software on the host.
package environment
