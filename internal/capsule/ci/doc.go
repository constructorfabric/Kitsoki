// Package ci implements story-native Capsule CI routing. Its project manifest
// chooses a story, environment, executor, and result contract; it deliberately
// does not define a second YAML step/DAG language.
package ci
