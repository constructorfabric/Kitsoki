---
filed_at: "2026-07-06T10:02:20Z"
status: open
title: "1187"
---
# 1187

## Comment 2026-07-06T10:02:20Z by kitsoki



### Reproduction artifact: 1187

## Bug Verified: `studioImportResolver` hard-errors on library stories when `--stories-dir` is set

### What is broken

`studioImportResolver` in `cmd/kitsoki/mcp.go` builds a closure over the base resolver. When `--stories-dir` is set and the import resolver is called with `override=true` for a story **not present in storiesDir**, the closure immediately returns a hard error:

```
--stories-dir=<dir>: story "dev-story" not found (looked for <dir>/dev-story/app.yaml): stat …: no such file or directory
```

The `ImportResolver` contract (documented in `internal/app/imports.go`) states that `override=true` should return `("", nil)` to fall through to the next tier (on-disk checkout, then embedded library) when no explicit override is configured for that name. The `--stories-dir` wrapper violates this contract: it treats _any_ story absent from `storiesDir` as a hard error, regardless of whether `$KITSOKI_REPO`, the embedded library, or an on-disk checkout could satisfy it.

### Affected call paths

Both `session.new` and `story.graph` ultimately call `app.LoadWithResolver(storyPath, nil, srv.importResolver)` where `srv.importResolver` is `studioImportResolver(storiesDir)`. Any downstream root story (e.g. a project instance under `.kitsoki/stories/`) that imports `@kitsoki/dev-story` fails to load when `kitsoki mcp --stories-dir <dir>` is running and `dev-story` is not in `<dir>`.

### How reproduced

A minimal deterministic test was written (`cmd/kitsoki/resolver_bug1187_repro_test.go`):
1. Sets `KITSOKI_REPO` to the kitsoki worktree (so a correct fall-through resolves `dev-story`).
2. Creates a `storiesDir` containing only a custom user story (no `dev-story`).
3. Creates a downstream `app.yaml` (in a temp dir outside the checkout) that declares `imports.core.source: "@kitsoki/dev-story"`.
4. Calls `app.LoadWithResolver(downstreamApp, nil, studioImportResolver(storiesDir))`.
5. Asserts the load **succeeds** — which it does NOT on the unfixed tree.

### Root cause (buggy line)

```go
// cmd/kitsoki/mcp.go, studioImportResolver — the override=true branch:
if _, err := os.Stat(candidate); err != nil {
    // BUG: hard error instead of falling through
    return "", fmt.Errorf("--stories-dir=%s: story %q not found (looked for %s): %w",
        storiesDir, name, candidate, err)
}
```

### Fix (verified by temporarily applying it)

Replace the hard error with a delegation to the base resolver:
```go
return base(name, importerDir, override)
```

This lets `$KITSOKI_REPO`, kit-dev overrides, and the embedded library satisfy imports that `storiesDir` doesn't cover — which is the correct layering.

### Evidence

- Test file committed: `cmd/kitsoki/resolver_bug1187_repro_test.go` at commit `52e455b2`
- RED on unfixed tree: error is `imports.core: source "@kitsoki/dev-story": --stories-dir=…: story "dev-story" not found`
- GREEN after one-line fix: load succeeds and `def.States["core"]` is populated

_phase: reproducing_1187_0_

## Comment 2026-07-06T10:07:18Z by kitsoki


### Fix proposal: 1187

... (full markdown above)

_phase: proposing_1187_0_

## Comment 2026-07-06T10:35:04Z by kitsoki

### Test review: 1187

## Fix reviewed

The fix is a one-line change in `cmd/kitsoki/mcp.go`: inside `studioImportResolver`, when a story is **absent** from `storiesDir` and `override=true`, the old code returned a hard `fmt.Errorf("--stories-dir=…: story %q not found …")`; the fix replaces that with `return base(name, importerDir, override)`, delegating to the base resolver (which checks `$KITSOKI_REPO`, kitdev overrides, and the embedded library). This is exactly the one-line fix proposed in the reproduction artifact.

## Tests added / modified

**New file — `cmd/kitsoki/resolver_bug1187_repro_test.go`** (140 lines, committed in the RED-reproducer commit `52e455b2`, now GREEN):

- `TestStudioImportResolver_StoriesDirFallsThruForLibraryStory` — the primary end-to-end regression test. It sets `KITSOKI_REPO` to the local checkout, creates a `storiesDir` that contains only a custom user story (no `dev-story`), builds a downstream `app.yaml` in a separate temp dir that declares `imports.core.source: "@kitsoki/dev-story"`, then calls `app.LoadWithResolver(downstreamApp, nil, studioImportResolver(storiesDir))` — the exact call path used by both `session.new` and `story.graph`. It asserts (a) no error, (b) non-nil `AppDef`, and (c) `def.States["core"]` is present, confirming that `dev-story` was actually folded under the `core` alias and not just that the error was swallowed.
- `TestStudioImportResolver_StoriesDirStillOverridesWhenPresent` — complementary regression guard confirming that stories **present** in `storiesDir` still resolve from there (the override path is not broken by the fall-through).

**Modified — `cmd/kitsoki/mcp_studio_harness_test.go`**: The existing `TestStudioImportResolverStoriesDir` had asserted that a missing story produces an error containing `"--stories-dir="`. That assertion was documenting the **buggy** behaviour. The fix flips it: with `KITSOKI_REPO` cleared so the base returns `("", nil)`, the test now asserts `require.NoError` + `require.Empty(path)` — proving the resolver no longer emits a `--stories-dir` hard error for absent stories.

## CI run result

The full CI log is clean: all 100+ packages report `ok`, including `kitsoki/cmd/kitsoki` (20.729s). No `FAIL` lines, no panics, no build errors. Every test that previously passed continues to pass; the previously-RED reproducer is now GREEN.

## End-to-end outcome assessment

The ticket's observable deliverable is: *a downstream root story importing `@kitsoki/dev-story` must load successfully when `kitsoki mcp --stories-dir` is running and `dev-story` is not in that directory*. `TestStudioImportResolver_StoriesDirFallsThruForLibraryStory` asserts this directly via `app.LoadWithResolver` — the identical call made by `session.new` and `story.graph` — and checks the far-side outcome (`def.States["core"]` populated), not merely that no error was returned. This is a genuine end-to-end assertion rather than a near-side mechanism check. No blockers.

_phase: testing_1187_0_
