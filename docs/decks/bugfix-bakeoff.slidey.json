{
  "_comment": "Bugfix bake-off narrative deck. Scene types (title / cards / narrative) match stories/slidey-edit/baked/deck.json + stories/slidey-edit/schemas/deck.json so it bakes to static HTML via `slidey bundle` (see deck/README.md). Numbers are the real bug9 results from results/cells/*.json; pending grid cells are marked 'pending (quota/queue)', not invented.",
  "meta": {
    "title": "The Bugfix Bake-off",
    "resolution": { "width": 1920, "height": 1080 },
    "theme": "rose-pine-moon",
    "narration": { "voice": "en-AU-NatashaNeural", "rate": "+0%" }
  },
  "scenes": [
    {
      "type": "title",
      "eyebrow": "Kitsoki case study",
      "title": "The Bugfix Bake-off",
      "subtitle": "Is structure worth more than a bigger model?",
      "narration": "We hold the bugs fixed and vary two things — the structure of the workflow, and the model behind it — then grade every run against a hidden oracle."
    },
    {
      "type": "narrative",
      "eyebrow": "The thesis",
      "lede": "Two axes, one question.",
      "body": "Does the kitsoki pipeline make fixes better, and how does that trade against the model you point at it? We vary structure — the seven-room bugfix pipeline versus a single multi-stage prompt — and model — GLM-5.2, Opus 4.8, Sonnet 4.6, GPT-5.5 — over the same real bugs. If a cheaper model with structure matches a frontier model without it, structure is the lever and model is just the cost knob.",
      "narration": "The question those leave open: does structure make fixes better, and how does that trade against the model? We vary the workflow structure and the model over the same real bugs."
    },
    {
      "type": "cards",
      "variant": "grid",
      "title": "The experiment: a 2×4 grid over 5 bugs",
      "narration": "For each real bug we run a two-by-four grid: two treatments by four candidates, all from the fix's hermetic parent commit, graded against the fix's own regression test as a hidden oracle — with adjudication for oracles that are coupled to canonical wording.",
      "cards": [
        { "label": "2 treatments", "sub": "kitsoki pipeline vs single prompt", "style": "primary" },
        { "label": "4 candidates", "sub": "GLM-5.2 · Opus 4.8 · Sonnet 4.6 · GPT-5.5", "style": "secondary" },
        { "label": "5 real bugs", "sub": "hermetic parent-commit baselines", "style": "default" },
        { "label": "hidden oracle", "sub": "fix's own test + behavioural adjudication", "style": "default" }
      ]
    },
    {
      "type": "narrative",
      "eyebrow": "Method · honesty controls",
      "lede": "No self-report. One cost basis.",
      "body": "Each cell runs in its own disposable worktree from the fix's parent commit, pre-flighted RED. The hidden oracle stays out of the candidate's tree until scoring. Wording-coupled oracles false-fail correct fixes, so a judge adjudicates on behaviour — keeping the raw oracle status. Cost is one basis: kitsoki's native per-call cost, and claude -p priced from recorded usage on a corrected rate table that reproduces native cost to within half a percent.",
      "narration": "Each cell is hermetic and pre-flighted red. The oracle is hidden until scoring, behavioural adjudication rescues correct fixes done differently, and cost is reported on one consistent basis with tokens as the primary axis."
    },
    {
      "type": "cards",
      "variant": "grid",
      "title": "First results — bug9 (P1: shared-checkout WIP destruction)",
      "narration": "Three completed cells, all on bug nine, all adjudicated solves over an oracle that false-fails on canonical wording.",
      "cards": [
        { "label": "opus · single", "sub": "solved · 0 turns · $4.00 · 3.5M tok · 299s", "style": "primary" },
        { "label": "sonnet · single", "sub": "solved · 1 turn · $3.02 · 3.2M tok · 288s", "style": "secondary" },
        { "label": "opus · kitsoki", "sub": "solved · $7.73 · 4.0M tok · 1056s · +regression test", "style": "default" },
        { "label": "rest of grid", "sub": "pending (quota / queue)", "style": "default" }
      ]
    },
    {
      "type": "narrative",
      "eyebrow": "Takeaway · structure vs cost",
      "lede": "Structure is not automatically cheaper.",
      "body": "On bug9, the kitsoki pipeline on Opus cost $7.73 — about 1.9× the same model under a single prompt at $4.00, and roughly 3.5× the wall time. The pipeline's overhead is real. Structure buys thoroughness, not a smaller bill, when the single prompt happens to land a correct fix unaided.",
      "narration": "Structure is not automatically cheaper. The pipeline on Opus cost nearly twice the same model under a single prompt. The overhead is real."
    },
    {
      "type": "narrative",
      "eyebrow": "Takeaway · structure is more thorough",
      "lede": "Only the pipeline parked safely.",
      "body": "The kitsoki cell chose a different valid fix — a per-session worktree path, the ticket's own proposed resolution — wrote its own regression test, ran a refine loop, and parked at a red gate instead of declaring victory. The single-prompt cells solved the behaviour but added no regression test and left the suite red. The pipeline never shipped an unverified fix.",
      "narration": "Only the pipeline wrote its own regression test, ran a refine loop, and parked at a red gate. The single-prompt runs solved the behaviour but left the suite red and added no test."
    },
    {
      "type": "narrative",
      "eyebrow": "Takeaway · the model axis",
      "lede": "A cheaper model matched the frontier one.",
      "body": "Sonnet under a single prompt reached the same behavioural solve as Opus for $3.02 with one feedback turn. The model axis is at least as consequential as the structure axis on this bug — which is exactly the trade the study is built to measure across the full grid.",
      "narration": "Sonnet matched Opus on the same bug for three dollars with one feedback turn. The model axis matters as much as structure."
    },
    {
      "type": "cards",
      "variant": "grid",
      "title": "The dogfood payoff: the pipeline caught a bad fix",
      "narration": "Building the kitsoki treatment was a dogfood marathon. The headline finding: the hardened pipeline catches fixes a naive single prompt ships unverified.",
      "cards": [
        { "label": "F1 — caught & hardened", "sub": "blind implementer shipped a 65-test-breaking fix → self-verify before submit (d210ea67)", "style": "primary" },
        { "label": "F2 — under-reported parking", "sub": "park reason hides 'breaks N tests'", "style": "secondary" },
        { "label": "F3 — uncleaned worktree", "sub": "park leaves branch + no resume hint", "style": "default" },
        { "label": "P1 — missing trace", "sub": "MCP sessions lack discoverable trace (filed)", "style": "default" }
      ]
    },
    {
      "type": "narrative",
      "eyebrow": "Status",
      "lede": "Methodology validated; full grid pending.",
      "body": "Three of forty cells are scored — all on bug9. GLM-5.2 cells are blocked on a synthetic.new quota wall. bug12, bug14, and the sonnet/glm/gpt kitsoki cells are queued. This is a validated method plus a first worked example, not a finished sweep — the empty cells are pending, not zero.",
      "narration": "Three of forty cells are scored. The GLM column is blocked on quota and the rest are queued. This is a validated method and a first result, not a finished sweep."
    },
    {
      "type": "narrative",
      "eyebrow": "Conclusion",
      "lede": "Structure is the lever. Model is the cost knob.",
      "body": "The framework is reusable: drop new bugs into bakeoff.yaml, point it at new candidates, regrade against hermetic baselines and hidden oracles, regenerate the report and deck offline with zero re-spend. The run distilled into two general skills — matrix-task-comparison and dogfood-marathon — with task-bakeoff and dogfood-marathon stories to follow.",
      "narration": "The framework is reusable — add bugs, add candidates, regrade offline with no re-spend. It distilled into two skills and two forthcoming stories, ready for the full grid."
    }
  ]
}
