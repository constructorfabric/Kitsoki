These are shared project subagents (Claude Code agent definitions). Codex/other
tools can read them here directly, and `make setup` symlinks them into the
project's `.claude/agents/` directory so Claude Code discovers them.
