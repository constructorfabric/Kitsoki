# Generated by tools/session-mining/flow_fixture_compiler.py (task 3.1).
# DO NOT EDIT BY HAND — regenerate from the source scenario IR document.
# Turns are one-per-mined-user-turn, each projected onto the fixed `ask`
# intent of stories/scenario-foundry-harness (slots.text drives the
# stubbed host.agent.converse call; display_input carries the operator's
# verbatim words for the trace/replay UI). See the module docstring for
# why this app rather than the source story's own intents.

test_kind: flow
app: ../../../../stories/scenario-foundry-harness/app.yaml
host_cassette: scn-f7b1da36-dd82-4111-93d8-706bf032e786-0000.cassette.yaml
initial_state: desk
initial_world: {}
turns:
  - intent: { name: ask, slots: { text: "i have a VM for the kitsoki-github-agent - gracefully shut it down" } }
    display_input: "i have a VM for the kitsoki-github-agent - gracefully shut it down"
    expect_state: desk
    expect_world: { last_answer: "acknowledged \u2014 expected effects: Bash: ls cmd/kitsoki/gh_agent* && echo <val> && grep -rl <val> --i completed; Bash: ssh -o ConnectTimeout=10 -o BatchMode=yes root@<IP> completed; Bash: ssh root@<IP> <val>--- stopping service (graceful completed; Bash: ssh root@<IP> <val>syncing disks<val>scheduling po completed; Read: <path> completed" }
expect_no_errors: true
