# Generated by tools/session-mining/flow_fixture_compiler.py
# (compile_scenario_onto_workbench, room-workbench S6 "workbench-target").
# DO NOT EDIT BY HAND — regenerate from the source scenario IR document.
# Turns are one-per-mined-user-turn, each projected onto the target's
# real workbench: room's synthesized <room>_capture intent (slots.request
# drives the room's own on_enter host.agent.task dispatch; display_input
# carries the operator's verbatim words for the trace/replay UI). Only
# the LAST turn carries a world_override seeding the target's
# expected_effects world var — see the module docstring for why.

test_kind: flow
app: ../../../../stories/slidey-dev/app.yaml
host_cassette: scn-687e848b-baa7-4050-aaeb-d38a340fef5a-0000.slidey-dev.cassette.yaml
initial_state: core.landing
initial_world: {}
turns:
  - intent: { name: core__landing_capture, slots: { request: "why do we keep losing the mcp tools - i updated .<file> a few times an apparently it's gone again" } }
    display_input: "why do we keep losing the mcp tools - i updated .<file> a few times an apparently it's gone again"
    expect_state: core.landing
    expect_world: { core__landing_request: "why do we keep losing the mcp tools - i updated .<file> a few times an apparently it's gone again", core__landing_note: { summary: "noted \u2014 a correction followed (Bash: cd <path> git add .<file> && git commit)" } }
  - intent: { name: core__landing_capture, slots: { request: "commit it" } }
    display_input: "commit it"
    world_override: { core__landing_expected_effects: ["Bash: cat <path><file> 2>/dev/null; echo <va completed", "Bash: cd <path> echo <val>; grep -nE <val> .git completed", "Bash: cd <path> echo <val>; kitsoki mcp --help completed", "Bash: cd <path> echo <val>; ls -la .<file> 2> completed", "Bash: cd <path> git add .<file> && git commit completed", "Write: <path><file> completed"] }
    expect_state: core.landing
    expect_world: { core__landing_request: "commit it", core__landing_note: { summary: "acknowledged \u2014 expected effects: Bash: cat <path><file> 2>/dev/null; echo <va completed; Bash: cd <path> echo <val>; grep -nE <val> .git completed; Bash: cd <path> echo <val>; kitsoki mcp --help completed; Bash: cd <path> echo <val>; ls -la .<file> 2> completed; Bash: cd <path> git add .<file> && git commit completed; Write: <path><file> completed" } }
expect_no_errors: true
