# Generated by tools/session-mining/flow_fixture_compiler.py
# (compile_scenario_onto_workbench, room-workbench S6 "workbench-target").
# DO NOT EDIT BY HAND — regenerate from the source scenario IR document.
# Turns are one-per-mined-user-turn, each projected onto the target's
# real workbench: room's synthesized <room>_capture intent (slots.request
# drives the room's own on_enter host.agent.task dispatch; display_input
# carries the operator's verbatim words for the trace/replay UI). Only
# the LAST turn carries a world_override seeding the target's
# expected_effects world var — see the module docstring for why.

test_kind: flow
app: ../../../../stories/dev-story/app.yaml
host_cassette: scn-74252840-25f6-4449-85a0-8ad99f7c97d5-0002.dev-story.cassette.yaml
initial_state: landing
initial_world: {}
turns:
  - intent: { name: landing_capture, slots: { request: "if make install fails, give a message that maybe make setup is needed" } }
    display_input: "if make install fails, give a message that maybe make setup is needed"
    world_override: { landing_expected_effects: ["Bash: git add Makefile && git commit -m <val>EOF' build: hint `mak completed", "Bash: make check-deps && echo <val>; PATH=<val> make check-deps; e completed", "Edit: <path> completed", "Read: <path> completed"] }
    expect_state: landing
    expect_world: { landing_request: "if make install fails, give a message that maybe make setup is needed", landing_note: { summary: "acknowledged \u2014 expected effects: Bash: git add Makefile && git commit -m <val>EOF' build: hint `mak completed; Bash: make check-deps && echo <val>; PATH=<val> make check-deps; e completed; Edit: <path> completed; Read: <path> completed" } }
expect_no_errors: true
