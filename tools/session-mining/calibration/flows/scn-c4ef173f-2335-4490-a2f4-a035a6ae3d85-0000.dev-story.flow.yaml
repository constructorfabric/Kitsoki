# Generated by tools/session-mining/flow_fixture_compiler.py
# (compile_scenario_onto_workbench, room-workbench S6 "workbench-target").
# DO NOT EDIT BY HAND — regenerate from the source scenario IR document.
# Turns are one-per-mined-user-turn, each projected onto the target's
# real workbench: room's synthesized <room>_capture intent (slots.request
# drives the room's own on_enter host.agent.task dispatch; display_input
# carries the operator's verbatim words for the trace/replay UI). Only
# the LAST turn carries a world_override seeding the target's
# expected_effects world var — see the module docstring for why.

test_kind: flow
app: ../../../../stories/dev-story/app.yaml
host_cassette: scn-c4ef173f-2335-4490-a2f4-a035a6ae3d85-0000.dev-story.cassette.yaml
initial_state: landing
initial_world: {}
turns:
  - intent: { name: landing_capture, slots: { request: "check the .worktrees for things that are either garbage or already merged - i want to clean up to focus on real work" } }
    display_input: "check the .worktrees for things that are either garbage or already merged - i want to clean up to focus on real work"
    world_override: { landing_expected_effects: ["Bash: cd <path> echo <val> git cherry main web-b completed", "Bash: cd <path> echo <val> git diff --stat main. completed", "Bash: cd <path> echo <val>; git merge-base --is- completed", "Bash: cd <path> for wt in bf-B-x bf-F-2026-06-11 completed", "Bash: cd <path> git worktree add .worktrees/merg completed", "Bash: cd <path> git worktree remove --force .wor completed", "Bash: ls -la .worktrees/ 2>/dev/null && echo <val> && git worktree completed"] }
    expect_state: landing
    expect_world: { landing_request: "check the .worktrees for things that are either garbage or already merged - i want to clean up to focus on real work", landing_note: { summary: "acknowledged \u2014 expected effects: Bash: cd <path> echo <val> git cherry main web-b completed; Bash: cd <path> echo <val> git diff --stat main. completed; Bash: cd <path> echo <val>; git merge-base --is- completed; Bash: cd <path> for wt in bf-B-x bf-F-2026-06-11 completed; Bash: cd <path> git worktree add .worktrees/merg completed; Bash: cd <path> git worktree remove --force .wor completed; Bash: ls -la .worktrees/ 2>/dev/null && echo <val> && git worktree completed" } }
expect_no_errors: true
