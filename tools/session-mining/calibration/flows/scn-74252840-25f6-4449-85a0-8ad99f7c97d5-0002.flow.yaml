# Generated by tools/session-mining/flow_fixture_compiler.py (task 3.1).
# DO NOT EDIT BY HAND — regenerate from the source scenario IR document.
# Turns are one-per-mined-user-turn, each projected onto the fixed `ask`
# intent of stories/scenario-foundry-harness (slots.text drives the
# stubbed host.agent.converse call; display_input carries the operator's
# verbatim words for the trace/replay UI). See the module docstring for
# why this app rather than the source story's own intents.

test_kind: flow
app: ../../../../stories/scenario-foundry-harness/app.yaml
host_cassette: scn-74252840-25f6-4449-85a0-8ad99f7c97d5-0002.cassette.yaml
initial_state: desk
initial_world: {}
turns:
  - intent: { name: ask, slots: { text: "if make install fails, give a message that maybe make setup is needed" } }
    display_input: "if make install fails, give a message that maybe make setup is needed"
    expect_state: desk
    expect_world: { last_answer: "acknowledged \u2014 expected effects: Bash: git add Makefile && git commit -m <val>EOF' build: hint `mak completed; Bash: make check-deps && echo <val>; PATH=<val> make check-deps; e completed; Edit: <path> completed; Read: <path> completed" }
expect_no_errors: true
