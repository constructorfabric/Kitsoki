# Generated by tools/session-mining/flow_fixture_compiler.py
# (compile_scenario_onto_workbench, room-workbench S6 "workbench-target").
# DO NOT EDIT BY HAND — regenerate from the source scenario IR document.
# One host.agent.task episode per mined turn (sequential — the i-th
# dispatch consumes the i-th episode), whose submitted.summary is
# canned_answer(scenario, i) — the same deterministic derivation the
# ask-intent harness path uses, never invented content.

kind: host_cassette
app_id: pets-dev
app_version: "0.1.0"
match_on: [handler]

episodes:
  - id: dispatch_1
    match: { handler: host.agent.task }
    response:
      data:
        ok: true
        submitted:
          summary: "noted \u2014 a correction followed (Bash: cd <path> git add .<file> && git commit)"
  - id: dispatch_2
    match: { handler: host.agent.task }
    response:
      data:
        ok: true
        submitted:
          summary: "acknowledged \u2014 expected effects: Bash: cat <path><file> 2>/dev/null; echo <va completed; Bash: cd <path> echo <val>; grep -nE <val> .git completed; Bash: cd <path> echo <val>; kitsoki mcp --help completed; Bash: cd <path> echo <val>; ls -la .<file> 2> completed; Bash: cd <path> git add .<file> && git commit completed; Write: <path><file> completed"
