# Generated by tools/session-mining/flow_fixture_compiler.py
# (compile_scenario_onto_workbench, room-workbench S6 "workbench-target").
# DO NOT EDIT BY HAND — regenerate from the source scenario IR document.
# Turns are one-per-mined-user-turn, each projected onto the target's
# real workbench: room's synthesized <room>_capture intent (slots.request
# drives the room's own on_enter host.agent.task dispatch; display_input
# carries the operator's verbatim words for the trace/replay UI). Only
# the LAST turn carries a world_override seeding the target's
# expected_effects world var — see the module docstring for why.

test_kind: flow
app: ../../../../stories/dev-story/app.yaml
host_cassette: scn-1b4ace86-f192-43b0-ab86-16142fec0079-0001.dev-story.cassette.yaml
initial_state: landing
initial_world: {}
turns:
  - intent: { name: landing_capture, slots: { request: "resolve the red gate test that's already written but not committed" } }
    display_input: "resolve the red gate test that's already written but not committed"
    world_override: { landing_expected_effects: ["Bash: echo <val> && git ls-files internal/mcp/studio/session_live_ completed", "Bash: echo <val> && git status --short && echo <val> && for wt in completed"] }
    expect_state: landing
    expect_world: { landing_request: "resolve the red gate test that's already written but not committed", landing_note: { summary: "acknowledged \u2014 expected effects: Bash: echo <val> && git ls-files internal/mcp/studio/session_live_ completed; Bash: echo <val> && git status --short && echo <val> && for wt in completed" } }
expect_no_errors: true
