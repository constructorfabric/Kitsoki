# Generated by tools/session-mining/flow_fixture_compiler.py (task 3.1).
# DO NOT EDIT BY HAND — regenerate from the source scenario IR document.
# Turns are one-per-mined-user-turn, each projected onto the fixed `ask`
# intent of stories/scenario-foundry-harness (slots.text drives the
# stubbed host.agent.converse call; display_input carries the operator's
# verbatim words for the trace/replay UI). See the module docstring for
# why this app rather than the source story's own intents.

test_kind: flow
app: ../../../../stories/scenario-foundry-harness/app.yaml
host_cassette: scn-74252840-25f6-4449-85a0-8ad99f7c97d5-0000.cassette.yaml
initial_state: desk
initial_world: {}
turns:
  - intent: { name: ask, slots: { text: "this repo is fresh on this machine - i want make install to run cleanly, there should be some make setup job that will install everything necessary so users don't have to do it - cover mac, rockylinux and debian/ubuntu style" } }
    display_input: "this repo is fresh on this machine - i want make install to run cleanly, there should be some make setup job that will install everything necessary so users don't have to do it - cover mac, rockylinux and debian/ubuntu style"
    expect_state: desk
    expect_world: { last_answer: "acknowledged \u2014 expected effects: Bash: git add scripts/<file> Makefile && git status --short && g completed; Bash: git commit -m <val>EOF' build: add `make setup` to install d completed; Bash: ls -la && cat Makefile 2>/dev/null | head -100 completed; Bash: ls test/e2e; echo <val>; find test/e2e -name <val> -exec cat completed; Bash: make install 2>&1 | tail -15 completed; Bash: make setup 2>&1 | tail -25 completed; Bash: tail -n +100 Makefile; echo <val>; head -5 go.mod; echo <val completed; Edit: <path> completed; Write: <path> completed" }
expect_no_errors: true
