# Generated by tools/session-mining/flow_fixture_compiler.py (task 3.1).
# DO NOT EDIT BY HAND — regenerate from the source scenario IR document.
# Turns are one-per-mined-user-turn, each projected onto the fixed `ask`
# intent of stories/scenario-foundry-harness (slots.text drives the
# stubbed host.agent.converse call; display_input carries the operator's
# verbatim words for the trace/replay UI). See the module docstring for
# why this app rather than the source story's own intents.

test_kind: flow
app: ../../../../stories/scenario-foundry-harness/app.yaml
host_cassette: scn-rollout-2026-06-20t13-01-55-019ee39f-18dc-73f1-9f35-859bb770bfd2-0000.cassette.yaml
initial_state: desk
initial_world: {}
turns:
  - intent: { name: ask, slots: { text: "somehow main got checked out to a fix branch - this keeps happening despite intended guards - get back to main and make sure anything on this branch moves over to main" } }
    display_input: "somehow main got checked out to a fix branch - this keeps happening despite intended guards - get back to main and make sure anything on this branch moves over to main"
    expect_state: desk
    expect_world: { last_answer: "acknowledged \u2014 expected effects: exec_command: git branch --merged main completed; exec_command: git branch -d fix/vscode-embed-compact-scaling completed; exec_command: git merge --ff-only fix/vscode-embed-compact-scaling completed; exec_command: git status --short --branch completed; exec_command: git switch main completed" }
expect_no_errors: true
