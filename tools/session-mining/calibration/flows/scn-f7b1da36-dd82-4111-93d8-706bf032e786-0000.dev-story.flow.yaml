# Generated by tools/session-mining/flow_fixture_compiler.py
# (compile_scenario_onto_workbench, room-workbench S6 "workbench-target").
# DO NOT EDIT BY HAND — regenerate from the source scenario IR document.
# Turns are one-per-mined-user-turn, each projected onto the target's
# real workbench: room's synthesized <room>_capture intent (slots.request
# drives the room's own on_enter host.agent.task dispatch; display_input
# carries the operator's verbatim words for the trace/replay UI). Only
# the LAST turn carries a world_override seeding the target's
# expected_effects world var — see the module docstring for why.

test_kind: flow
app: ../../../../stories/dev-story/app.yaml
host_cassette: scn-f7b1da36-dd82-4111-93d8-706bf032e786-0000.dev-story.cassette.yaml
initial_state: landing
initial_world: {}
turns:
  - intent: { name: landing_capture, slots: { request: "i have a VM for the kitsoki-github-agent - gracefully shut it down" } }
    display_input: "i have a VM for the kitsoki-github-agent - gracefully shut it down"
    world_override: { landing_expected_effects: ["Bash: ls cmd/kitsoki/gh_agent* && echo <val> && grep -rl <val> --i completed", "Bash: ssh -o ConnectTimeout=10 -o BatchMode=yes root@<IP> completed", "Bash: ssh root@<IP> <val>--- stopping service (graceful completed", "Bash: ssh root@<IP> <val>syncing disks<val>scheduling po completed", "Read: <path> completed"] }
    expect_state: landing
    expect_world: { landing_request: "i have a VM for the kitsoki-github-agent - gracefully shut it down", landing_note: { summary: "acknowledged \u2014 expected effects: Bash: ls cmd/kitsoki/gh_agent* && echo <val> && grep -rl <val> --i completed; Bash: ssh -o ConnectTimeout=10 -o BatchMode=yes root@<IP> completed; Bash: ssh root@<IP> <val>--- stopping service (graceful completed; Bash: ssh root@<IP> <val>syncing disks<val>scheduling po completed; Read: <path> completed" } }
expect_no_errors: true
