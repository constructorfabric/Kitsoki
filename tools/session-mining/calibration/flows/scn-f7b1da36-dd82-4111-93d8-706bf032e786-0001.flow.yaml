# Generated by tools/session-mining/flow_fixture_compiler.py (task 3.1).
# DO NOT EDIT BY HAND — regenerate from the source scenario IR document.
# Turns are one-per-mined-user-turn, each projected onto the fixed `ask`
# intent of stories/scenario-foundry-harness (slots.text drives the
# stubbed host.agent.converse call; display_input carries the operator's
# verbatim words for the trace/replay UI). See the module docstring for
# why this app rather than the source story's own intents.

test_kind: flow
app: ../../../../stories/scenario-foundry-harness/app.yaml
host_cassette: scn-f7b1da36-dd82-4111-93d8-706bf032e786-0001.cassette.yaml
initial_state: desk
initial_world: {}
turns:
  - intent: { name: ask, slots: { text: "ok it has been resized - i turned it on, check that it starts up properly" } }
    display_input: "ok it has been resized - i turned it on, check that it starts up properly"
    expect_state: desk
    expect_world: { last_answer: "acknowledged \u2014 expected effects: Bash: for i in $(seq 1 6); do ssh -o ConnectTimeout=8 -o BatchMode completed; Bash: ssh root@<IP> <val>=== local listener ===<val> [lo completed; Bash: ssh root@<IP> <val>=== resources (post-resize) === completed" }
expect_no_errors: true
