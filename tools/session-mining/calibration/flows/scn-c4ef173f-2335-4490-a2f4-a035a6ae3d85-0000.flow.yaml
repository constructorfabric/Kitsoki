# Generated by tools/session-mining/flow_fixture_compiler.py (task 3.1).
# DO NOT EDIT BY HAND — regenerate from the source scenario IR document.
# Turns are one-per-mined-user-turn, each projected onto the fixed `ask`
# intent of stories/scenario-foundry-harness (slots.text drives the
# stubbed host.agent.converse call; display_input carries the operator's
# verbatim words for the trace/replay UI). See the module docstring for
# why this app rather than the source story's own intents.

test_kind: flow
app: ../../../../stories/scenario-foundry-harness/app.yaml
host_cassette: scn-c4ef173f-2335-4490-a2f4-a035a6ae3d85-0000.cassette.yaml
initial_state: desk
initial_world: {}
turns:
  - intent: { name: ask, slots: { text: "check the .worktrees for things that are either garbage or already merged - i want to clean up to focus on real work" } }
    display_input: "check the .worktrees for things that are either garbage or already merged - i want to clean up to focus on real work"
    expect_state: desk
    expect_world: { last_answer: "acknowledged \u2014 expected effects: Bash: cd <path> echo <val> git cherry main web-b completed; Bash: cd <path> echo <val> git diff --stat main. completed; Bash: cd <path> echo <val>; git merge-base --is- completed; Bash: cd <path> for wt in bf-B-x bf-F-2026-06-11 completed; Bash: cd <path> git worktree add .worktrees/merg completed; Bash: cd <path> git worktree remove --force .wor completed; Bash: ls -la .worktrees/ 2>/dev/null && echo <val> && git worktree completed" }
expect_no_errors: true
