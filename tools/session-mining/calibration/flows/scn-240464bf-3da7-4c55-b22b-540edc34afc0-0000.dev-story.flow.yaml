# Generated by tools/session-mining/flow_fixture_compiler.py
# (compile_scenario_onto_workbench, room-workbench S6 "workbench-target").
# DO NOT EDIT BY HAND — regenerate from the source scenario IR document.
# Turns are one-per-mined-user-turn, each projected onto the target's
# real workbench: room's synthesized <room>_capture intent (slots.request
# drives the room's own on_enter host.agent.task dispatch; display_input
# carries the operator's verbatim words for the trace/replay UI). Only
# the LAST turn carries a world_override seeding the target's
# expected_effects world var — see the module docstring for why.

test_kind: flow
app: ../../../../stories/dev-story/app.yaml
host_cassette: scn-240464bf-3da7-4c55-b22b-540edc34afc0-0000.dev-story.cassette.yaml
initial_state: landing
initial_world: {}
turns:
  - intent: { name: landing_capture, slots: { request: "just file the ticket, we'll handle it async, need to commit the bugs to the repo for now - we'll evolve it before we release widely" } }
    display_input: "just file the ticket, we'll handle it async, need to commit the bugs to the repo for now - we'll evolve it before we release widely"
    world_override: { landing_expected_effects: ["Edit: <path> completed"] }
    expect_state: landing
    expect_world: { landing_request: "just file the ticket, we'll handle it async, need to commit the bugs to the repo for now - we'll evolve it before we release widely", landing_note: { summary: "acknowledged \u2014 expected effects: Edit: <path> completed" } }
expect_no_errors: true
