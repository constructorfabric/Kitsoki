# Generated by tools/session-mining/flow_fixture_compiler.py
# (compile_scenario_onto_workbench, room-workbench S6 "workbench-target").
# DO NOT EDIT BY HAND — regenerate from the source scenario IR document.
# Turns are one-per-mined-user-turn, each projected onto the target's
# real workbench: room's synthesized <room>_capture intent (slots.request
# drives the room's own on_enter host.agent.task dispatch; display_input
# carries the operator's verbatim words for the trace/replay UI). Only
# the LAST turn carries a world_override seeding the target's
# expected_effects world var — see the module docstring for why.

test_kind: flow
app: ../../../../stories/pets-dev/app.yaml
host_cassette: scn-74252840-25f6-4449-85a0-8ad99f7c97d5-0000.pets-dev.cassette.yaml
initial_state: core.landing
initial_world: {}
turns:
  - intent: { name: core__landing_capture, slots: { request: "this repo is fresh on this machine - i want make install to run cleanly, there should be some make setup job that will install everything necessary so users don't have to do it - cover mac, rockylinux and debian/ubuntu style" } }
    display_input: "this repo is fresh on this machine - i want make install to run cleanly, there should be some make setup job that will install everything necessary so users don't have to do it - cover mac, rockylinux and debian/ubuntu style"
    world_override: { core__landing_expected_effects: ["Bash: git add scripts/<file> Makefile && git status --short && g completed", "Bash: git commit -m <val>EOF' build: add `make setup` to install d completed", "Bash: ls -la && cat Makefile 2>/dev/null | head -100 completed", "Bash: ls test/e2e; echo <val>; find test/e2e -name <val> -exec cat completed", "Bash: make install 2>&1 | tail -15 completed", "Bash: make setup 2>&1 | tail -25 completed", "Bash: tail -n +100 Makefile; echo <val>; head -5 go.mod; echo <val completed", "Edit: <path> completed", "Write: <path> completed"] }
    expect_state: core.landing
    expect_world: { core__landing_request: "this repo is fresh on this machine - i want make install to run cleanly, there should be some make setup job that will install everything necessary so users don't have to do it - cover mac, rockylinux and debian/ubuntu style", core__landing_note: { summary: "acknowledged \u2014 expected effects: Bash: git add scripts/<file> Makefile && git status --short && g completed; Bash: git commit -m <val>EOF' build: add `make setup` to install d completed; Bash: ls -la && cat Makefile 2>/dev/null | head -100 completed; Bash: ls test/e2e; echo <val>; find test/e2e -name <val> -exec cat completed; Bash: make install 2>&1 | tail -15 completed; Bash: make setup 2>&1 | tail -25 completed; Bash: tail -n +100 Makefile; echo <val>; head -5 go.mod; echo <val completed; Edit: <path> completed; Write: <path> completed" } }
expect_no_errors: true
