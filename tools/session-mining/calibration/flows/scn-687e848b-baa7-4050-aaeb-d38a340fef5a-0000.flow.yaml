# Generated by tools/session-mining/flow_fixture_compiler.py (task 3.1).
# DO NOT EDIT BY HAND — regenerate from the source scenario IR document.
# Turns are one-per-mined-user-turn, each projected onto the fixed `ask`
# intent of stories/scenario-foundry-harness (slots.text drives the
# stubbed host.agent.converse call; display_input carries the operator's
# verbatim words for the trace/replay UI). See the module docstring for
# why this app rather than the source story's own intents.

test_kind: flow
app: ../../../../stories/scenario-foundry-harness/app.yaml
host_cassette: scn-687e848b-baa7-4050-aaeb-d38a340fef5a-0000.cassette.yaml
initial_state: desk
initial_world: {}
turns:
  - intent: { name: ask, slots: { text: "why do we keep losing the mcp tools - i updated .<file> a few times an apparently it's gone again" } }
    display_input: "why do we keep losing the mcp tools - i updated .<file> a few times an apparently it's gone again"
    expect_state: desk
    expect_world: { last_answer: "noted \u2014 a correction followed (Bash: cd <path> git add .<file> && git commit)" }
  - intent: { name: ask, slots: { text: "commit it" } }
    display_input: "commit it"
    expect_state: desk
    expect_world: { last_answer: "acknowledged \u2014 expected effects: Bash: cat <path><file> 2>/dev/null; echo <va completed; Bash: cd <path> echo <val>; grep -nE <val> .git completed; Bash: cd <path> echo <val>; kitsoki mcp --help completed; Bash: cd <path> echo <val>; ls -la .<file> 2> completed; Bash: cd <path> git add .<file> && git commit completed; Write: <path><file> completed" }
expect_no_errors: true
