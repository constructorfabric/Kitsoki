# Generated by tools/session-mining/flow_fixture_compiler.py (task 3.1).
# DO NOT EDIT BY HAND — regenerate from the source scenario IR document.
# Turns are one-per-mined-user-turn, each projected onto the fixed `ask`
# intent of stories/scenario-foundry-harness (slots.text drives the
# stubbed host.agent.converse call; display_input carries the operator's
# verbatim words for the trace/replay UI). See the module docstring for
# why this app rather than the source story's own intents.

test_kind: flow
app: ../../../../stories/scenario-foundry-harness/app.yaml
host_cassette: scn-2e107a60-10e3-4d70-afe5-ea995adc4d6f-0000.cassette.yaml
initial_state: desk
initial_world: {}
turns:
  - intent: { name: ask, slots: { text: "i want to dogfood kitsoki to build a critical new feature - we need to create a standardized per-agent definition that can provide mcp, permissions, prompt, etc... some of that is already in the room, but each agent invocation (like agent.task for example) needs the ability to specify an agent with an exact set of mcps (tools too), permissions, model/harness, etc..." } }
    display_input: "i want to dogfood kitsoki to build a critical new feature - we need to create a standardized per-agent definition that can provide mcp, permissions, prompt, etc... some of that is already in the room, but each agent invocation (like agent.task for example) needs the ability to specify an agent with an exact set of mcps (tools too), permissions, model/harness, etc..."
    expect_state: desk
    expect_world: { last_answer: "acknowledged \u2014 expected effects: mcp__kitsoki__story_graph: {<val>:<val>} completed; mcp__kitsoki__story_read: {<val>:<val>,<val>:<val>} completed; mcp__kitsoki__studio_handles: {} completed; mcp__kitsoki__studio_ping: {} completed" }
expect_no_errors: true
