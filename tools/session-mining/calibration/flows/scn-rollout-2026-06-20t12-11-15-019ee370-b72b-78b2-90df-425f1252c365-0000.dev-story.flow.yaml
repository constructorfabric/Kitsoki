# Generated by tools/session-mining/flow_fixture_compiler.py
# (compile_scenario_onto_workbench, room-workbench S6 "workbench-target").
# DO NOT EDIT BY HAND — regenerate from the source scenario IR document.
# Turns are one-per-mined-user-turn, each projected onto the target's
# real workbench: room's synthesized <room>_capture intent (slots.request
# drives the room's own on_enter host.agent.task dispatch; display_input
# carries the operator's verbatim words for the trace/replay UI). Only
# the LAST turn carries a world_override seeding the target's
# expected_effects world var — see the module docstring for why.

test_kind: flow
app: ../../../../stories/dev-story/app.yaml
host_cassette: scn-rollout-2026-06-20t12-11-15-019ee370-b72b-78b2-90df-425f1252c365-0000.dev-story.cassette.yaml
initial_state: landing
initial_world: {}
turns:
  - intent: { name: landing_capture, slots: { request: "this folder is supposed to be pinned to the main branch - it somehow got checked out to a different branch - get me back on main and make sure everything from this branch is merged in" } }
    display_input: "this folder is supposed to be pinned to the main branch - it somehow got checked out to a different branch - get me back on main and make sure everything from this branch is merged in"
    world_override: { landing_expected_effects: ["exec_command: git log --oneline -n 3 completed", "exec_command: git merge --ff-only fix/vscode-chat-story-picker completed", "exec_command: git rev-parse --short HEAD completed", "exec_command: git status --short --branch completed", "exec_command: git switch main completed"] }
    expect_state: landing
    expect_world: { landing_request: "this folder is supposed to be pinned to the main branch - it somehow got checked out to a different branch - get me back on main and make sure everything from this branch is merged in", landing_note: { summary: "acknowledged \u2014 expected effects: exec_command: git log --oneline -n 3 completed; exec_command: git merge --ff-only fix/vscode-chat-story-picker completed; exec_command: git rev-parse --short HEAD completed; exec_command: git status --short --branch completed; exec_command: git switch main completed" } }
expect_no_errors: true
