The purpose of this viewer is to check the traces - the traces are the critical, auditable, trackable information.

We use this viewer to understand the validity of the traces.  It is critical that the trace itself is accurate - never use a UI hack or adjustment to cover for a problem in the trace - the trace itself must always be correct.
